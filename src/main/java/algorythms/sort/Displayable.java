package algorythms.sort;

/**
 * Whenever object implements this interface this means
 * it can display its information in some way
 */
public interface Displayable {
    /**
     * Print result in console
     */
    void display();
}
